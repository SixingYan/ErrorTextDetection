def train(tloader, vloader, model, args):
    model.train()

    for epoch in range():
        for x_batch, y_batch in tqdm(train_loader, disable=True)

        model(torch.tensor(x_batch, dtype=torch.long))
